apiVersion: v1
kind: Secret
metadata:
  name: retrogemini-smtp
  labels:
    app: retrogemini
type: Opaque
stringData:
  # SMTP configuration for email invitations and password reset
  # If SMTP_HOST is empty or not set, email features are disabled
  SMTP_HOST: ""
  SMTP_PORT: "587"
  SMTP_SECURE: "false"
  SMTP_USER: ""
  SMTP_PASS: ""
  FROM_EMAIL: ""
