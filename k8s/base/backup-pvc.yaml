apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: retrogemini-backups
  labels:
    app: retrogemini
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  # storageClassName: standard  # Uncomment and adjust for your cluster
